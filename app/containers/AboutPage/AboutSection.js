import styled from 'styled-components';

const AboutSection = styled.div`
  border-bottom: #bebfc0 solid 2px;
  display: inherit;
  flex-direction: row;
  padding: 1.9rem 0;
`;

export default AboutSection;
